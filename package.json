{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rspress build",
    "dev": "rspress dev",
    "format": "prettier . --write --cache",
    "preview": "rspress preview"
  },
  "dependencies": {
    "rspress": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^16",
    "prettier": "^3.0.0"
  }
}
